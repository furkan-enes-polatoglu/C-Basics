#include <stdio.h>
#include <math.h>
#include <locale.h>
int main()
{
	setlocale(LC_ALL,"Turkish");
	
	/*
	int sayi;
	do
	{
		printf("Sayý giriniz : "); scanf("%d",&sayi);
		printf("%d sayýsýnýn 2 katý = %d\n",sayi,sayi*sayi);
	}
	while(sayi>0);
	// sayý 0'dan büyük olduðu sürece yukarýdaki iþlemi tekrarla
	printf("Program sonlandý!");
	*/
	
	
	
	/*
	int sayi;
	do
	{
		printf("Sayý giriniz : "); scanf("%d",&sayi);
		printf("%d sayýsýnýn 2 katý = %d\n",sayi,sayi*sayi);
	}
	while(sayi!=-5);
	// girilen sayý -5 olmadýðý sürece sürekli iþlem yap
	printf("Program sonlandý!");
	*/

	
}
